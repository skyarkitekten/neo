# Shell hook scripts must be LF on every platform — a CRLF `.sh` fails on
# Linux/macOS bash with `$'\r': command not found`, breaking the hooks.
*.sh text eol=lf

.github/workflows/*.lock.yml linguist-generated=true merge=ours